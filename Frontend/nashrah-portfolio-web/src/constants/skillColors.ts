// Maps the existing category keys from constants/skills.ts to the
// portfolio's palette. Keys are unchanged so skills.ts doesn't need
// to be touched — only what each key renders as has changed.
export const categoryColors: Record<string, string> = {
  blue: "#7B93B8",
  green: "#7FA593",
  orange: "#C7A362",
  purple: "#9B8AC4",
  pink: "#C68FA0",
};
